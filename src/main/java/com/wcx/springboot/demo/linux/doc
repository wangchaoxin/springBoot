1 查找占用最大文件夹和文件
    -a  显示所有，包括文件
     du -a /home | sort -n -r | head -n 5
2 查找某个路径下所有文件是否包含某个字符串
    grep -r "abc" .
    grep -r "abc" *
3 递归查找文件
     find . -name "foo*"
     不区分大小写
     find . -print | grep -i foo