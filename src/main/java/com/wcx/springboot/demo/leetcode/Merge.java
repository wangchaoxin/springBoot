package com.wcx.springboot.demo.leetcode;

public class Merge {
    public static void main(String[] args) {

    }

    public int[] merge(int[] a, int[] b) {
        if (a == null || a.length == 0)
            return b;
        if (b == null || b.length == 0)
            return a;
        int al = a.length;
        int bl = b.length;
        int[] c = new int[a.length + b.length];
        int i, j, k = 0;
      /*  while (i < al && j < bl) {

        }*/
        return new int[1];
    }
}
